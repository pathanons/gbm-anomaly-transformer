# Attention diagnostics for x-1 windows before each A/AKAM k=10 MAD overlap spike.
# Red marker = selected previous window (x-1); orange dashed line = reference spike (x).
pipeline: attention_visualize
exp_name: best_nllassoc_w100_h2_l4_lr0p00005_ep20_s42
output_root: D:/AnomalyTransformerRuns
data_path: datasets/SP500_event_taxonomy_w{window_size}
tickers: all
device: auto
features: all
normalize_batch: false
include_anomalous_train: false
split_method: chronological
batch_size: 32
window_size: 100
step: 1
seed: 42
d_model: 128
n_heads: 2
e_layers: 4
d_ff: 256
dropout: 0.1
predictive_distribution: gaussian
association_mode: gaussian_log_return
loss_mode: legacy
patience: 5
epochs: 20
lr: 0.00005
dist_weight: 1.0
recon_weight: 0.0
divergence_weight: 0.0
association_weight: 0.1
score_mode: legacy
quantile_count: 21
tail_weight_gamma: 2.0
tail_weight_power: 2.0
attention_split: test
attention_tickers: A,AKAM
attention_select_by: delta_mad_previous
attention_mad_k: 10.0
attention_top_k: all
attention_overlap_only: true
attention_mask_diagonal: true
attention_out: D:/AnomalyTransformerRuns/experiments/best_nllassoc_w100_h2_l4_lr0p00005_ep20_s42/reports/attention_previous_delta_mad10_overlap_A_AKAM_all_layers_heads_no_diag
attention_layer: all
attention_head: all
attention_make_plots: true
price_dir: datasets/SP500
price_z_thr: 3.0
