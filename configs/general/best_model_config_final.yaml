# Final remembered best model config.
# Uses the existing checkpoint from best_nllassoc_w100_h2_l4_lr0p00005_ep40_s99
# and writes fresh test artifacts to a new experiment folder.
pipeline: test
exp_name: best_model_config_final
checkpoint_exp_name: best_nllassoc_w100_h2_l4_lr0p00005_ep40_s99
output_root: D:/AnomalyTransformerRuns
data_path: datasets/SP500_event_taxonomy_w{window_size}
tickers: all
device: auto
features: all
normalize_batch: false
include_anomalous_train: false
split_method: chronological
purge_gap: null
batch_size: 32
window_size: 100
step: 1
seed: 99
d_model: 128
n_heads: 2
e_layers: 4
d_ff: 256
dropout: 0.1
predictive_distribution: gaussian
association_mode: gaussian_log_return
loss_mode: legacy
patience: 5
epochs: 40
lr: 0.00005
dist_weight: 1.0
recon_weight: 0.0
divergence_weight: 0.0
association_weight: 0.1
score_mode: legacy
quantile_count: 21
tail_weight_gamma: 2.0
tail_weight_power: 2.0
test_label_mode: endpoint_log_return_3std
